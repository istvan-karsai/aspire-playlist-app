:80 {
    # Proxy API and Scalar documentation traffic to the C# Web API
    handle /api/* {
        reverse_proxy api:8080
    }

    handle /scalar/* {
        reverse_proxy api:8080
    }

    # Proxy all other web traffic to the React frontend container
    handle {
        reverse_proxy frontend:80
    }
}